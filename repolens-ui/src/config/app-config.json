{
  "development": {
    "api": {
      "baseUrl": "http://localhost:5179",
      "timeout": 30000,
      "retryAttempts": 3
    },
    "auth": {
      "tokenStorageKey": "repolens_token",
      "refreshThreshold": 300000
    },
    "ui": {
      "theme": "light",
      "pageSize": 20,
      "refreshInterval": 30000
    }
  },
  "production": {
    "api": {
      "baseUrl": "https://api.repolens.com",
      "timeout": 30000,
      "retryAttempts": 3
    },
    "auth": {
      "tokenStorageKey": "repolens_token",
      "refreshThreshold": 300000
    },
    "ui": {
      "theme": "light",
      "pageSize": 20,
      "refreshInterval": 60000
    }
  }
}
